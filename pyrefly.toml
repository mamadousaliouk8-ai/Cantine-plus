# Pyrefly configuration for Cantine+
# This tells Pyrefly where to look for Python modules

[project]
# Include only the backend directory for Python analysis
include = ["backend/**/*.py"]

[python]
# Add backend as a search path so `from db import ...` resolves correctly
search_path = ["backend"]
