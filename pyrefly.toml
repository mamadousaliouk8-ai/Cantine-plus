# Root pyrefly config — covers Python files outside backend/ (scripts, data, etc.)
# The backend/ directory has its own pyrefly.toml anchored to backend/ as the import root.

[project]
include = ["*.py", "data/**/*.py"]
